# Session Learnings

This directory contains knowledge captured by Claude before context compaction.
Each file represents one compaction event, named: `YYYY-MM-DDTHH-MM-{slug}.md`

These files are re-injected after compaction so learnings carry forward across sessions.
