// Main entry point for CSS and global styles
import './index.css';

// This module serves as the main entry point for the application's CSS
// It imports the base CSS file which includes Tailwind directives and custom styles
// This ensures CSS is processed by Vite and included in the build correctly
